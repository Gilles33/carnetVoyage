<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}<link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/css/app.css">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

    </head>
    <body>
    <a class="btn btn-info" href="{{ path('carnetVoyage') }}">Accueil</a>
    <a class="btn btn-success" href="{{ path('destination_index') }}">Destinations</a>
<a class="btn btn-primary" href="{{ path('step_index') }}">Étapes</a>
<a class="btn btn-warning" href="{{ path('link_index') }}">Ressources</a>
        {% block body %}{% endblock %}
        {% block javascripts %}<script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
            <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
            {% endblock %}
    </body>
</html>
